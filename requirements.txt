streamlit>=1.28.0
pandas>=2.1.0
numpy>=1.24.0
requests>=2.31.0
python-dotenv>=1.0.0
scikit-learn>=1.3.0
plotly>=5.18.0
matplotlib>=3.8.0
flask>=3.0.0
flask-cors>=4.0.0
joblib>=1.3.0
shap>=0.43.0
xgboost>=2.0.0

# Optional packages for local/Docker training and Hopsworks usage.
# These are intentionally not included in the main deployment requirements because
# TensorFlow and Hopsworks are large and sometimes fail during hosted app installs.
# Install them in a local environment with:
#   pip install -r requirements-optional.txt